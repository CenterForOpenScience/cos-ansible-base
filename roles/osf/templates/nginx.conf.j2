server {
    listen   80;
    server_name  {{inventory_hostname}};

    access_log  {{nginx_log_dir}}/default.access.log;

    location / {
        include uwsgi_params;
        uwsgi_pass unix://{{ osf_uwsgi_socket }};
    }
}
